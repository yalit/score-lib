{% extends 'main.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('listing_show') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('listing_show') }}
{% endblock %}

{% block title %}{{ 'listing.index.title' | trans }}{% endblock %}

{#
{% block body %}
    {{ include('includes/_card.html.twig', {
        'card_class': "mt-6",
        'card_title': card.getTitle("library.index.title"),
        'card_content': _self.getContent(scores)
    }) }}
{% endblock %}
#}
